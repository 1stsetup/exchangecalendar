if ( typeof (rtews) == "undefined")
    var rtews = {};

/*
 * Utility methods
 *
 */
rtews.Utils = {
    log : function(title, msg) {
        dump("\n[RemoteTaggerEWS]: " + title + " **: " + msg + "\n");
    }
};
